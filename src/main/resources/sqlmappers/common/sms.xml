<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="sms">

	<!-- 生成账户信息 -->
	<insert id="insertSmsOperWater" parameterType="Map">
		INSERT INTO a_sms (
			smsSequence,
			branchSequence,
			operTime,
			smsJson,
			smsCode,
			templateCode,
			phoneNum,
			singName,
			productName,
			responseCode
		)VALUES(
			#{smsSequence},
			#{branchSequence},
			#{operTime},
			#{smsJson},
			#{smsCode},
			#{templateCode},
			#{phoneNum},
			#{singName},
			#{productName},
			#{responseCode}
		)
	</insert>

</mapper>